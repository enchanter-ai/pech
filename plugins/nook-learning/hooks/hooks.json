{
  "description": "nook-learning hooks — accumulate patterns before compaction wipes in-memory session state. Advisory-only.",
  "hooks": {
    "PreCompact": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 ${CLAUDE_PLUGIN_ROOT}/shared/scripts/accumulate_pattern.py",
            "timeout": 15
          }
        ]
      }
    ]
  }
}
